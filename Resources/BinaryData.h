/* Binary data embedded in the plugin */
#pragma once

namespace BinaryData
{
    // Simple orange square icon data (16x16 PNG)
    // This is a base64 encoded minimal PNG
    extern const char* icon_png;
    const int icon_pngSize = 0; // Will be set if we have actual data
}
